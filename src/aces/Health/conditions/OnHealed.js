export const config = {
  listName: "On healed",
  displayText: "On healed",
  description: "Trigger when the instance is healed.",
  isTrigger: true,
  isInvertible: false,
  params: [],
};

export const expose = true;

export default function () {
  return true;
}
