export const config = {
  listName: "On death",
  displayText: "On death",
  description: "Trigger when the instance dies.",
  isTrigger: true,
  isInvertible: false,
  params: [],
};

export const expose = true;

export default function () {
  return true;
}
